{
  "scripts": {
    "start": "nodemon server.js",
    "mongod": "mongod --fork --dbpath var/data --logpath var/log/mongod.log --logappend --pidfilepath `pwd`/var/run/mongod.pid"
  },
  "dependencies": {
    "graphql": "^0.12.3",
    "koa": "^2.4.1",
    "koa-graphql": "^0.7.5",
    "koa-mount": "^3.0.0",
    "koa-router": "^7.4.0",
    "koa-session": "^5.8.1",
    "koa-static": "^4.0.2",
    "mongoose": "^5.0.3"
  }
}
